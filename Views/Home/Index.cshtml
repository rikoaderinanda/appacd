@{
    ViewData["Title"] = "Home Page";
    ViewData["Header"] = "1";
    ViewData["Footer"] = "1";
}

@await Html.PartialAsync("_ModalAddressManage")
<div class="app-body pb-30">
    <div class="container">
        <div class="row">
            @await Html.PartialAsync("_headerName")
            @await Html.PartialAsync("_layanan")
            @await Html.PartialAsync("_promo")
        </div>
    </div>
</div>
